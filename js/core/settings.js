// app settings
window.settings = {
    trace: true,
    // soundcard settings
    /*input: internalSoundcard,
    output: internalSoundcard,
    sampleRate: 44100,
    bufferSize: 512,
    latency: 0.1, // in seconds
    audioEnabled: true,

    // UI settings
    theme: 'light', // 'light' or 'dark'*/
};

console.log('Settings initialized:', settings);